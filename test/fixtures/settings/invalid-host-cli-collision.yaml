version: 1
project:
  name: forge-collision
tracker:
  type: linear
  config:
    team_id: TEAM-123
secrets:
  manager: env_file
agents:
  # FORGE-88: collision fixture uses codex/codex — both are valid enum members
  # under the post-FORGE-88 schema (claude is no longer a review_host_cli
  # value), so this fixture proves the refinement-level collision check, not
  # the enum-level value check.
  primary_host_cli: codex
  review_host_cli: codex
