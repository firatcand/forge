version: 1
project:
  name: forge
  description: Lightweight framework for agentic development — wraps Claude Code (or Codex / Cursor / Gemini) with structured discovery, decomposition, and Symphony-style parallel agent orchestration.
tracker:
  type: linear
  config:
    team_id: "bf9e034a-51fc-4147-99ea-692947dcc1bb"
secrets:
  manager: env_file
  env_file_path: ./.env.local
agents:
  max_concurrent: 10
  retry_attempts: 10
  retry_backoff_ms_max: 300000
  poll_interval_ms: 30000
  worktree_root: ./.forge/worktrees
  on_persistent_failure: notify
  primary_host_cli: claude
  review_host_cli: codex
  # FORGE-152: this repo dogfoods with only CLAUDE.md (since the framework
  # itself is iterated primarily via Claude Code). Adopters who want Codex /
  # Gemini support add their kind here and re-run forge init / forge upgrade.
  enabled_root_files:
    - claude
design:
  mode: project_owned
