# {{PROJECT_NAME}} — Project Context

> **Stub.** This file is the canonical synthesis of THIS project's spec
> (BRIEF + PRD + SPEC + DESIGN) and is auto-loaded into every agent session as
> the project's "what". It has not been populated yet.
>
> Run `/ingest-spec` to validate the spec chain and generate the real content
> here. Until then, agents should read `spec/BRIEF.md`, `spec/PRD.md`, and
> `spec/SPEC.md` directly for project context.

_No synthesized context yet — run `/ingest-spec`._
