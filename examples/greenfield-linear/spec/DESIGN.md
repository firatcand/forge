# Greenfield DESIGN

> Compact example design system (project-owned mode).

Tokens, components, and layout rules live here. This stub exists so the example
tree mirrors a real Forge project; it carries no `@inherit` lines (v0.4+).
